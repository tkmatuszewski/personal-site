import styled from "styled-components";

const HeroBackLight = styled.div`
    grid-row: 2/3;
    grid-column: 2/3;
    justify-self: center;
    align-self: center;
    width: 70%;
    height: 60vh;
    border-radius: 40%;
    background: rgba(208, 208, 208, 0.15);
    box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
    filter: blur(100px);
`

export default HeroBackLight